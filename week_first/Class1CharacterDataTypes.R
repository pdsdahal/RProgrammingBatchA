# R- Character DataTypes

countryName <- "United States"
is.character(countryName) #TRUE, it checks whether the variable countryName is a character type.
typeof(countryName) #character, function to check the data type of a variable:
class(countryName) #character, function to check the data type of a variable

nchar(countryName) # length of character string in a variable use in the nchar
